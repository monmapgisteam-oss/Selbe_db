# Даалгавар — CEO-гийн самбарт «Ирээдүйд тулгамдаж буй асуудал» хэсэг нэмэх

> **Урьтал:** `CEO_KPI_PROMPT.md` (5 ангилал + 3 түвшний гэрлэн дохио). Энэ нь
> тэр самбарт нэмэгдэх **шинэ, зургаа дахь блок**. Тэр даалгавар дуусаагүй бол
> эхлээд түүнийг хийнэ.
>
> **Мөн чанар:** Тохиромжтой байдлын шинжилгээ (Suitability) аль хэдийн бүсийн
> хэмжээнд норм зөрчлийг илрүүлдэг. Гэвч CEO нь бүсээр биш **багцаар** ажилладаг.
> Энэ хэсэг тэр хоёрыг холбоно: *багц тус бүрд ямар норм зөрчигдөж байна, яагаад,
> хэр яаралтай.*

---

## 1. Яагаад «ирээдүйд» вэ — энэ хэсгийн үндсэн санаа

Тохиромжтой байдлын үнэлгээ нь **төлөвлөгдсөн** төлөв байдлыг хэмждэг. Бүс нь
ногооны нормыг зөрчиж байна гэдэг нь өнөөдрийн асуудал БИШ — тэр барилга бариад
дуусахад **бетон дотор хатах** асуудал.

Тиймээс энэ хэсэг нь бусад дөрвөн ангиллаас цаг хугацааны хувьд ялгаатай:

| Бусад ангилал | Энэ хэсэг |
|---|---|
| Аль хэдийн болсон зүйл | **Хараахан болоогүй, гэвч зайлшгүй болох** зүйл |
| Засах = нөхөх | **Засах = зураг төслийг өөрчлөх** |
| Хойшлуулбал өртөг нэмэгдэнэ | **Хойшлуулбал засах боломж БҮРМӨСӨН хаагдана** |

### ⭐ Гол механизм — «засах боломжийн цонх»

Норм зөрчлийг засах өртөг нь **барилгын явцаас урвуу хамааралтай**:

- 5% гүйцэтгэлтэй багц дээрх зөрчил — зураг төслийн засвар, харьцангуй хямд.
- 60% гүйцэтгэлтэй багц дээрх ижил зөрчил — практикт засагдахгүй.

Тиймээс энэ хэсгийн эрэмбэлэлт нь **зөвхөн зөрчлийн хэмжээгээр биш**, зөрчил ба
үлдсэн боломжийн ҮРЖВЭРЭЭР явна (§6). Энэ бол хэсгийн гол үнэ цэн: CEO-д «юу
буруу байна» гэдгийг биш, **«юуг нь одоо засвал болох вэ»** гэдгийг хэлнэ.

---

## 2. Одоо байгаа Suitability архитектур

| Файл | Юу байгаа |
|---|---|
| `src/lib/analysis/config.ts` | `INDICATORS` (9 үзүүлэлт) · `SCORE_LEVELS` · `STRICT_NORM` · `NORM_FAIL_MAX = 44` · `DENSITY_BY_TYPE` (Хүснэгт 6.1) · `ASSUME_MET` |
| `src/lib/analysis/score.ts` | `urbanScore()` → `{score, parts}` · `scoreIndicator()` · **`passesNorm()`** · `normFor()` · `normText()` · `scoreLabel()` |
| `src/lib/analysis/data.ts` | `loadAnalysisCached()` → `AnalysisData.zones: Zone[]` · `computeRaw()` · `computeEconomics()` · `Zone.raw` / **`Zone.rawActual`** · `SocialResult.parts` |
| `src/lib/execData.ts` | `useSuitability()` → `SuitSummary` — одоо зөвхөн `avgScore`, `levels`, `ranked`, `byId {score, type}` буцаана |
| `src/lib/services.ts` | `PKG_BY_BAGTS` · `PKG_ZONED` · `ZONE_FIELDS` · `BUILT_FIELDS` · `bagtsKey()` · `buildingKey()` |
| `src/lib/blockProgress.ts` | `loadBlockProgress()` → `BlockProgressMap` (блокийн гүйцэтгэл) |

### Есөн үзүүлэлт ба тэдгээрийн жин

| id | Нэр | Горим | Жин | Норм |
|---|---|---|---|---|
| `density` | Хүн амын нягтшил | band | **24** | БНБД 6.9 — 300–450 хүн/га |
| `far` | FAR | lower | **19** | БНБД Хүснэгт 6.1 — бүсийн төрлөөр |
| `green` | Нэг хүнд ногдох ногоон | higher | **16** | БНБД Хүснэгт 8.2 — ≥6.0 м²/хүн |
| `parking` | Зогсоолын хангамж | higher | **10** | БНБД 10.32 — 1.0/өрх |
| `bcr` | BCR | lower | **8** | БНБД Хүснэгт 6.1 |
| `social` | Нийгмийн дэд бүтцийн хүртээмж | higher | **8** | дотоод 500 м буфер |
| `engineering` | Инженерийн дэд бүтцийн хүртээмж | lower | **7** | ⚠️ батлагдаагүй таамаг |
| `greenCap`, `densityCap` | — | — | 0 (`ref`) | оноололд ОРОХГҮЙ |

---

## 3. «Бага оноо» гэдгийг ЯЖ тодорхойлох вэ

**Дур зоргын босго ЗОХИОХГҮЙ.** Код аль хэдийн зөв тодорхойлолттой:

```
passesNorm(value, indicator) === false   →   норм ЗӨРЧСӨН
```

`STRICT_NORM = true` тул норм зөрчсөн үзүүлэлт дээд тал нь `NORM_FAIL_MAX = 44`
оноо авдаг — «Дунд»-ын босго (45)-аас доогуур, өөрөөр хэлбэл **норм зөрчсөн бүх
зүйл аль хэдийн улаан бүсэд байна**. Тиймээс:

- **🔴 bad** = `passesNorm() === false` (норм зөрчсөн)
- **🟡 warn** = норм хангасан ч захад нь — нормын **10% дотор** (жишээ: ногоон 6.0–6.6 м²/хүн)
- **🟢 good** = норм хангасан, захад биш
- **⚪ unknown** = `passesNorm() === null` (утга байхгүй)

> ⚠️ Шар түвшний 10% нь ШИНЭ тогтмол — `kpiLevels.ts`-д `NORM_MARGIN = 0.10`
> гэж нэрлэж, «нормын захад» гэсэн утгыг тайлбартай нь бичнэ.

### ⚠️⚠️ ХАМГИЙН ЧУХАЛ АЛДААНААС СЭРГИЙЛЭХ

`ASSUME_MET = { social: 100, engineering: 100 }` нь `Zone.raw`-д тэр хоёр
үзүүлэлтийг **хүчээр «норм хангасан» болгодог**. Хэрэв энэ хэсэг `Zone.raw`
уншвал:

> **Есөн үзүүлэлтийн хоёр нь хэзээ ч асуудал болж харагдахгүй.**

Бодит утга `Zone.rawActual`-д бүтнээрээ хадгалагдана.

**→ Энэ хэсэг ЗААВАЛ `rawActual` уншина.** (§10-C-г үз — энэ нь хэрэглэгчийн
2026-08-12-ны шийдвэртэй зөрчилдөж болзошгүй тул баталгаажуулах.)

---

## 4. ⚠️ Гол техникийн бэрхшээл — багц ↔ бүс холбоос

**Suitability нь БҮСЭЭР (52 бүс, `ZONE_ID`) бодогддог. `Zone` төрөлд багцын
талбар БАЙХГҮЙ.** Энэ холбоосыг байгуулах нь даалгаврын хамгийн эрсдэлтэй хэсэг.

Гурван боломж:

| # | Арга | Давуу тал | Сул тал |
|---|---|---|---|
| **A** | **Орон зайн огтлолцол** — багцын геометрийг 52 бүсийн полигонтой огтлох | Бүх багцад ажиллана; геометр нь маргаангүй | Хүнд тооцоо (`loadOverlaps`-ын жишиг бий) |
| B | `PKG_ZONED` — багцын давхаргын `ZONE_ID` атрибут | Хямд, шууд | **Зөвхөн 15 давхаргад** `ZONE_ID` бий — бусад нь CAD-аас, харьяалалгүй |
| C | Барилгаар дамжуулан: ЕТ `barilga`.`Блокы` ↔ `BUILDING`.`BLOK` → `BAGTS`; `barilga`-д `ZONE_ID` бий | Хямд | Блокийн кодын бичиглэл хоёр үйлчилгээнд зөрж болно (`DATA_DICTIONARY`-ийн `bagts32_5_1` → `5/1` анхааруулгыг үз) |

**Санал: A (орон зайн), B-г шалгалт болгон.** Хоёрын үр дүн зөрвөл ил гаргана.

**Заавал гаргах баталгаажуулалтын тоо** (консол эсвэл `*.check.mjs`-д):
- Хэдэн багц бүстэй холбогдов / нийт хэдэн багц
- Хэдэн бүс ямар ч багцад хамаараагүй
- Хэдэн бүс **олон багцад** хамаарав (энэ нь ХЭВИЙН — давхардлыг зөв зохицуулах)

> ⚠️ **Дундажлахгүй.** Нэг багц олон бүстэй, нэг бүс олон багцтай. Багцын бүх
> бүсийн оноог дундажлавал хамгийн муу бүс далдлагдана — энэ хэсгийн зорилготой
> ЯГ ЭСРЭГ. Багц бүрд түүний **зөрчилтэй бүсүүдийг НЭР ЗААЖ** жагсаана.

---

## 5. Шалтгааны загвар — үзүүлэлт бүрээр

«Ногоон бага» гэдэг мэдээлэл; «Ногоон 2.1 м²/хүн (норм 6.0) — 4,200 оршин суугчид
8,800 м² ногоон» гэдэг шалтгаан. Шалтгаан бүр **`Zone`-ийн бодит талбараас**
гарна, ямар ч таамаг оруулахгүй:

| Үзүүлэлт | Шалтгааны эх сурвалж (`Zone`-оос) | Мөрийн загвар |
|---|---|---|
| `green` | `greenM2`, `residentPop` | «{green} м² ногоон ÷ {pop} оршин суугч = {v} м²/хүн, норм ≥6.0 — {gap} м²/хүн дутуу» |
| `density` | `residentPop`, `polyHa` | «{pop} хүн ÷ {ha} га = {v} хүн/га, норм 300–450 — {доогуур/дээгүүр}» |
| `far` | `gfaM2`, `areaHa`, `type` | «FAR {v}, «{type}» бүсийн норм ≤{max} (Хүснэгт 6.1) — {gap} хэтэрсэн» |
| `bcr` | `builtM2`, `areaHa` | «Барилгын хөл {m2} м² ÷ {ha} га = {v}%, норм ≤{max}%» |
| `parking` | `parkingSupply`, `parkingNeed`, `parkingGap` | «{supply} зогсоол ÷ {need} хэрэгцээ = {v}% — {gap} зогсоол дутуу» |
| `social` | `social.parts` — хамрах хувь хамгийн бага нь + `nearest` | «{label} хамралт {cover}% — хамгийн ойр нь {nearest} м» |
| `engineering` | `engDistM` | «Инженерийн шугам хүртэл {v} м, норм ≤100 м» ⚠️ норм батлагдаагүй |

**Дүрэм:** шалтгааны мөр нь ҮРГЭЛЖ *(бодит утга) · (норм) · (зөрүү)* гурвыг
агуулна. Зөвхөн оноо харуулах нь хориотой — оноо нь шалтгаан хэлдэггүй.

---

## 6. Эрэмбэлэлт — «яаралтай засах» оноо

```
urgency = severity × weight × opportunity
```

| Хүчин зүйл | Тооцоо | Утга |
|---|---|---|
| `severity` | нормоос хэр хол — `1 − scoreIndicator(value, ind) / NORM_FAIL_MAX`, 0..1 | Зөрчил хэр гүн |
| `weight` | `INDICATORS[i].weight ÷ Σweight` | Тэр үзүүлэлт хэр чухал |
| `opportunity` | `1 − (багцын гүйцэтгэл ÷ 100)` — `loadBlockProgress()`-ээс | **Засах боломж хэр үлдсэн** |

> ⚠️ `opportunity` нь энэ томьёоны бүх утга учир. Түүнгүйгээр жагсаалт нь
> «хамгийн муу бүс» болно; түүнтэй бол **«хамгийн яаралтай шийдвэр»** болно.
> 90% баригдсан багцын зөрчил жагсаалтын доод талд байх нь ЗӨВ — тэр нь
> шийдвэрийн зүйл биш, тэмдэглэлийн зүйл.

> ⚠️ Гүйцэтгэл 100% (эсвэл ашиглалтад орсон) багцын зөрчлийг жагсаалтаас
> ХАСАХГҮЙ, тусад нь **«Засагдахгүй — цаашид анхаарах»** гэсэн эгнээнд үлдээнэ.
> Дараагийн төслийн сургамж болно.

---

## 7. UI бүтэц

CEO-гийн 5 ангиллын дараа, зургаа дахь блок:

```
Ирээдүйд тулгамдаж буй асуудал            🔴 4 · 🟡 7
└ Багц 3.2 · Гүйцэтгэл 8% · 🔴 2 зөрчил
  ├ 🔴 Хүн амын нягтшил · A-14 бүс
  │   612 хүн ÷ 1.4 га = 437 хүн/га … (норм 300–450)
  │   [Бүс рүү очих →]
  └ 🔴 Ногоон байгууламж · A-14, A-15 бүс
      8,800 м² ÷ 4,200 хүн = 2.1 м²/хүн (норм ≥6.0) — 3.9 дутуу
```

**Дүрэм:**

1. Багц бүр **эвхэгддэг** — нээлттэй үед зөрчлүүд, хаалттай үед тоолуур.
2. Багцын толгойд **гүйцэтгэлийн %** заавал — «засах боломж» нь энд харагдана.
3. Мөр бүрээс **тухайн бүс рүү** үсэрнэ (`analysis` view, бүс сонгосон байдлаар).
4. Анхдагчаар зөвхөн **🔴 нээлттэй**; 🟡 нь «бас {n} анхаарах» гэж эвхээстэй.
5. Багцын дараалал — багцын хамгийн өндөр `urgency`-ээр.

---

## 8. Гүйцэтгэлийн шаардлага

`useSuitability` нь аль хэдийн **ХҮНД** тооцоо (бүх бүсийн геометр, ногоон,
зогсоол, өртөг). Энэ хэсэг түүн дээр багцын холбоос нэмнэ.

- Одоо байгаа кэшийг (`loadAnalysisCached`, модулийн түвшний кэш) **дахин ашиглана** — шинэ ачаалалт үүсгэхгүй.
- Багц↔бүс холбоосыг **нэг удаа** бодож кэшлэнэ.
- `CEO_KPI_PROMPT.md`-ийн `afterIdle` хойшлуулах загварыг дагана — энэ блок хамгийн сүүлд ачаалагдана.
- `useSuitability` нь `parts`-ыг буцаадаггүй тул `SuitSummary`-г **өргөтгөнө** (шинэ хүсэлт нэмэхгүйгээр — өгөгдөл аль хэдийн санах ойд бий).

---

## 9. ХОРИГЛОХ зүйл

1. **Шинэ ArcGIS хүсэлт нэмэхгүй** — орон зайн огтлолцлоос бусад (тэр нь заавал бол нэг удаа, кэштэй).
2. **`Zone.raw` уншихгүй — `Zone.rawActual` уншина** (§3).
3. **Багцын бүсүүдийн оноог дундажлахгүй** (§4).
4. **Дур зоргын онооны босго зохиохгүй** — `passesNorm` нь ганц эх сурвалж (§3).
5. **`ref` үзүүлэлтийг (`greenCap`, `densityCap`) асуудал болгохгүй** — норм байхгүй.
6. **Оноог шалтгаангүй харуулахгүй** — мөр бүр утга·норм·зөрүүтэй (§5).
7. Hex өнгө бичихгүй; `tr()` алгасахгүй; `en.ts` шинэчилнэ.

---

## 10. ⚠️ Кодыг бичихээс ӨМНӨ хэрэглэгчээс асуух

**A. Багц↔бүс холбоосын арга** — A (орон зайн), B (`PKG_ZONED`), C (блокоор)? §4.
Энэ нь бүх хэсгийн үндэс тул таамаглаж болохгүй.

**B. Аль багцууд хамрагдах вэ?** Зөвхөн орон сууцны 7 багц уу (`useBagtsTable`
эдгээрийг мэднэ, гүйцэтгэлтэй нь), эсвэл `PKG_TABLE`-ийн бүх багц уу (дэд бүтэц,
нийгмийн)? Сүүлийнх нь бүрэн, гэхдээ дэд бүтцийн багцад «гүйцэтгэл» байхгүй тул
`opportunity` бодогдохгүй.

**C. `ASSUME_MET` ба хэрэглэгчийн өмнөх шийдвэр.** `config.ts:918`-д «ХЭРЭГЛЭГЧИД
ТЭМДЭГЛЭХГҮЙ — UI-д «таамаг» гэх мэт заалт гаргахгүй (хэрэглэгчийн шийдвэр,
2026-08-12)» гэж бичээстэй. Гэтэл энэ хэсэг `rawActual` уншвал `social` ба
`engineering` зөрчил ил гарна — өмнөх шийдвэртэй зөрчилдөж болзошгүй. Гурван сонголт:
  1. `rawActual` уншиж, хоёуланг нь ердийн зөрчил гэж харуулах
  2. `rawActual` уншиж, «норм батлагдаагүй» тэмдэглэгээтэй тусад нь эгнээнд
  3. `raw` уншиж, хоёрыг нь энэ хэсгээс бүрмөсөн хасах

**D. `engineering`-ийн норм.** 100/500 м нь `config.ts:472`-т **батлагдаагүй
таамаг** гэж тэмдэглэгдсэн. Батлагдаагүй нормоор «зөрчил» зарлаж, CEO-гээр
зураг төсөл өөрчлүүлэх нь эрсдэлтэй. Энэ үзүүлэлтийг оруулах уу?

---

## 11. Хүлээн авах шалгуур

- [ ] Багц↔бүс холбоосын баталгаажуулалтын тоо ил гарсан (§4): холбогдсон багц, харьяалалгүй бүс, олон багцтай бүс.
- [ ] Багц бүрийн зөрчил нь **бүсийн нэрээр** жагсаагдсан — дундаж оноо ХАРАГДАХГҮЙ.
- [ ] Зөрчлийн мөр бүр *утга · норм · зөрүү* гурвыг агуулсан.
- [ ] Эрэмбэ нь гүйцэтгэлээс хамаарч байгаа: багц бүрийн гүйцэтгэлийг гараар өөрчлөхөд дараалал шилжинэ.
- [ ] `rawActual` ашиглагдсан (эсвэл §10-C-ийн шийдвэрийн дагуу) — `social`/`engineering` чимээгүй алга болоогүй.
- [ ] Хуучин `useSuitability` хэрэглэгчид (ExecKpi-ийн «Хот төлөвлөлтийн оноо» карт, `Suitability.tsx`) эвдрээгүй.
- [ ] Шинэ ArcGIS хүсэлт нэмэгдээгүй (Network таб — хүсэлтийн тоо өмнөхтэй ижил).
- [ ] `npm run typecheck`, `npm run lint`, `npm run test`, `npm run build` дөрвүүлээ амжилттай.
- [ ] Гэрэл/харанхуй горим хоёуланд шалгагдсан; hex өнгө байхгүй.
