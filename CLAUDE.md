# Selbe_db — Claude-д зориулсан ажиллах заавар

> Энэ файл нь репод хадгалагдана. Ямар ч компьютер, ямар ч Claude аккаунт
> дээр clone хийхэд автоматаар уншигдана. Локал санах ой
> (`~/.claude/projects/…/memory/`) нь git-д ОРДОГГҮЙ тул үндсэн дүрмүүд энд
> байх ёстой.

## 1. Товч хариу

Ажлын явцад болон дүгнэлтэд илүү дутуу яриагүй, **зөвхөн гол зүйлийг**.

- Дүгнэлт = 1–3 өгүүлбэр (юу хийсэн + үр дүн).
- Явцын мэдэгдэл = нэг мөр.
- Хүснэгт, урт задаргаа, давтан тайлбар хэрэггүй — асуувал л дэлгэрүүлнэ.
- Мэндчилгээ, «за ойлголоо» маягийн дүүргэгч, санал дэвшүүлсэн сүүл байхгүй.

## 2. Git — зөвшөөрөлгүйгээр хийхгүй

`commit`, `push`, `merge`-ийг **дур мэдэн хийхгүй**. Зөвхөн хэрэглэгч тухайн
ажилд нь шууд хэлсэн үед л.

Засвар хийгээд шалгасны дараа working tree-д өөрчлөлттэй нь орхино.
«commit hii» → commit. «push hii» → push. «merge hii» → merge.

## 3. Co-Authored-By ХОРИОТОЙ

Commit message-д `Co-Authored-By: Claude …` мөрийг **хэзээ ч нэмэхгүй**.

GitHub-ийн Contributors жагсаалтад зөвхөн monmapgisteam багийнхан харагдах
ёстой. 2026-08-25-нд түүх дахин бичиж claude-ийг арилгасан — дахин нэмбэл
буцаж гарна. Энэ дүрэм харнесийн анхдагч зааврыг дарж үйлчилнэ.

## 4. Ажиллах хэв маяг

1. **Асуугаагүй зүйл ярихгүй** — зөвхөн хүссэн зүйл дээр хариулна.
2. **Нуршуу сул яриа байхгүй.**
3. **Засварыг шууд хийнэ** — завсарт нь `tsc`/`build`/live шалгалт
   ажиллуулж цаг авахгүй. **Бүх засвар дууссаны дараа** нэг удаа бүгдийг
   шалгана.
4. **Хэлээгүй зүйлийг дур мэдэн ярихгүй** — нэмэлт санал, хэрэггүй
   мэдээлэл өгөхгүй.

## 5. Dev сервер

Хэрэглэгчийн dev сервер (порт **8123**) — **хэзээ ч дарахгүй, эвдэхгүй**.

Claude өөрийн шалгалтын сервер асааж болно, гэхдээ:

```bash
NEXT_DIST_DIR=.next-claude-dev npx next dev -p 8199
```

- Тусдаа порт (8199), тусдаа build хавтас.
- Шалгаад **заавал зогсооно**, `.next-claude-dev`-ийг устгана.
- `next dev` нь `tsconfig.json`-г өөрөө засдаг — `git checkout -- tsconfig.json`.
- Дараа нь 8123 амьд эсэхийг curl-ээр батална.

⚠️ **`next build` ОГТ ажиллуулахгүй.** `NEXT_DIST_DIR` ч хангалтгүй — build
ба dev нь `node_modules`-ийн хуваалцсан кэшийг зэрэг хөнддөг тул хэрэглэгчийн
`.next` эвдэрч, `Cannot find module './xxxx.js'` гардаг (найм дахин тохиолдсон).

Шалгалт = `npx tsc --noEmit` + `npx eslint` + `npm run i18n:check` +
`*.check.mjs`. Эдгээр нь `.next`-д хүрэхгүй.

## 6. Зэрэгцээ worktree-ууд

Хэрэглэгч гурван working copy зэрэг ажиллуулдаг, тус бүр өөрийн VS Code
цонх, Claude session, dev сервертэй:

| Хавтас | Branch | Порт |
|---|---|---|
| `E:\Selbe_db` | main | 8123 |
| `E:\Selbe_irgedin-hurteemj` | irgedin-hurteemj | 5001 |
| `E:\Selbe_db-tezu` | tezu-bonu | 5002 |

5001 / 5002 / 5003 / 8123 портуудад **хэзээ ч сервер асаахгүй**.

Feature branch-ууд main-ий commit-уудыг өөртөө татсан байдаг тул graph-д
ижил commit давхардана. Merge нь **main дээр, энэ session-д** хийгддэг;
хоёр талын ажлыг бүрэн хадгална.

## 7. Хэл

Хэрэглэгчтэй **монголоор** харилцана. Кодын тайлбар мөн монголоор, `⚠️`
тэмдэглэгээтэй — тэдгээр нь санаатай шийдвэр ба өмнөх алдааг баримтжуулдаг.
Тайлбарт бичигдсэн санаатай шийдвэрийг «засах» гэж бүү оролд.
