# CLAUDE.md — Сэлбэ порталын хуримтлагдсан мэдлэг

Clone хийсэн газар бүрд Claude Code энэ файлыг АВТОМАТААР уншина — шинэ
машин, шинэ сешн тэгээс биш, эндээс эхэлнэ. Шинэ сургамж гарвал нэмнэ.

> Эх сурвалж нь Claude-ийн локал санах ой
> (`~/.claude/projects/<замын-түлхүүр>/memory/`) — репогийн гадна,
> компьютерийн замаар түлхүүрлэгддэг тул clone-той хамт явдаггүй.
> Энэ файл нь түүний НЭГТГЭСЭН хувилбар.

---

## Хариултаа маш товч байлга — зөвхөн гол зүйл, нуршихгүй


Ажлын явцад бичих хариултаа **товч** байлгах. Зөвхөн гол зүйлийг хэлнэ: юу
хийсэн, үр дүн нь юу. Урт тайлбар, хүснэгт, олон гарчиг, давтсан баталгаа
хэрэггүй.

**Why:** Хэрэглэгч 2026-08-21-нд шууд хэлсэн: «үйл ажиллагаа явж байхад чи ямар
илүү дутуу ярья, хэт нуршуу байж болохгүй — зөвхөн гол юмаа товч ойлгомжтой
байх ёстой». Өмнө нь би алхам бүрд урт тайлан бичиж байсан.

**How to apply:** Ердийн ажилд 1–3 өгүүлбэр. Тоо/үр дүнг нэг мөрөөр. Гарчиг,
жагсаалт зөвхөн үнэхээр шаардлагатай үед (олон салангид олдвор). Кодын
тайлбарт (comment) энэ хамаарахгүй — тэнд дэлгэрэнгүй хэвээр.

**Сул яриа ОГТ байхгүй:** магтаал, уучлалт, «одоо ингэж хийе», «маш сайн»,
хийх гэж буй зүйлээ урьдчилан тайлбарлах, хийсэн зүйлээ давтах — бүгд хэрэггүй.
Зөвхөн үр дүн ба шаардлагатай асуулт.

**Асуугаагүй зүйл яриагүй:** хэрэглэгчийн асуугаагүй, хэлээгүй сэдвийг өөрөө
нээхгүй; «энэ бас байна», «дараа нь ингэж болно» гэсэн санаачилга хэрэггүй.
Зөвхөн асуусанд нь хариулна (2026-08-25-ны заавар).

---

## commit / push / merge-ийг зөвшөөрөлгүй ХЭЗЭЭ Ч хийхгүй


`git commit`, `git push`, `git merge` (мөн `reset`, `rebase`, салбар солих) —
хэрэглэгч ТУСГАЙЛАН хэлээгүй бол хийхгүй. Кодоо бичээд ажлын модонд үлдээнэ.

**Why:** Хэрэглэгч 2026-08-21-нд шууд хэлсэн: «commit push merge дур мэдэн хийж
болохгүй». Өмнө нь би салбарыг зөвшөөрөлгүй commit хийж, тэнцүүлж байсан. Мөн
нэг репог 4 worktree (main · tezu-bonu · irgedin-hurteemj) зэрэг ашигладаг тул
git-ийн үйлдэл бусад сессэд шууд нөлөөлнө.

**How to apply:** Ажил дуусахад «дууслаа, өөрчлөгдсөн файлууд эдгээр» гэж
хэлээд зогсоно. Commit хийх эсэхийг хэрэглэгч өөрөө шийднэ. [[brief-replies]]

---

## Засварыг шууд хий — явцын дунд хэт их шалгалт хийхгүй, бүх засвар дууссаны дараа нэг удаа шалгуулна


Засварыг **шууд хийх**. Засвар хийж явах явцад хэт их шалгалт (tsc, eslint,
хөтөч, скриншот) хийж цаг авахгүй. Бүх засвар дууссаны дараа хэрэглэгч өөрөө
шалгуулна.

**Why:** Хэрэглэгч 2026-08-25-нд хэлсэн: «edit-ийг шууд хий, хэт их шалгалт
хийх шаардлагагүй, бүх edit дууссаны дараа угаас бүгдийг нь шалгуулна —
edit явж байхад хэт их шалгаж цаг авах шаардлагагүй». Өмнө нь би засвар бүрийн
дараа typecheck + lint + puppeteer ажиллуулж, нэг ажил олон минут авдаг байв.

**How to apply:** Засварыг хийгээд дуусга. Шалгалтыг зөвхөн хэрэглэгч хүсэхэд
эсвэл бүх ажил дуусаад нэг удаа. Мөн [[brief-replies]]-ийн дагуу асуугаагүй
зүйл яриагүй, өөрөө шийдээгүй зүйл нэмэхгүй.

---

## Ажил дуусахад давах 4 шалгалт — tsc · eslint · npm test · i18n-extract


Сэлбэ порталд ажил дуусахад ЗААВАЛ давах шалгалт:

```
npx tsc --noEmit
npx eslint src                 # 0 алдаа (warning-ууд хуучнаас үлдсэн)
npm test
node tools/i18n-extract.mjs    # дутуу 0, илүүдэл 0
```

Том UI өөрчлөлтийн дараа `npm run build` нэмж ажиллуул — `tsc` барьдаггүй
JSX/CSS алдааг статик export барина.

**Why:** Төсөл нь `output: 'export'` статик тул build алдаа зөвхөн байршуулах
үед илэрдэг. i18n нь тусдаа шалгуур — `tr()`-ээр ороогүй монгол текст эсвэл
`src/i18n/en.ts`-д дутуу түлхүүр байвал англи хувилбар эвдэрнэ.

**How to apply:** Дөрвүүлээ цэвэр болтол ажил дуусаагүй. Хоёр байнгын занга:
(1) `en.ts`-ийн СҮҮЛИЙН мөрөнд таслал тавихаа мартвал толь задарч БҮХ тест
унана; (2) `npm test` доторх `test:agent` нь `agent-proxy/node_modules`
суулгаагүй worktree дээр унана — энэ нь орчны асуудал, кодын биш
(`cd agent-proxy && npm i`). [[edit-directly]]

---

## ArcGIS REST-ийн байнга давтагддаг занга — 200-аар ирдэг алдаа, хуудаслалт, хаалттай 2 үйлчилгээ


- **Алдаа нь HTTP 200-аар ирнэ.** `{ error: {...} }` биеийг ЗААВАЛ шалга;
  `res.ok` хангалтгүй.
- **2000 мөрийн хуудаслалт.** `resultOffset`/`resultRecordCount`-ыг
  `orderByFields`-гүй хэрэглэвэл давхардсан/алдагдсан мөр гарна.
- Огноо: `timestamp 'YYYY-MM-DD HH:MM:SS'`. Юникод мөр: `N'…'` угтвар.
- **Хоёр үйлчилгээ ХААЛТТАЙ (499)**: `Selbe_guitsetgel_consolidated` ба
  `Selbe_ET_20260721`. 127 давхарга бүр ил `url`-тай тул ET рүү унах
  fallback хэзээ ч ажиллахгүй.
- `applyEdits` нь 500 мөрийн багцаар явна; `rollbackOnFailure` нь ЗӨВХӨН нэг
  багц дотор үйлчилнэ (`bagtsSheet.applyUpdates`).

**Why:** Эдгээр нь тус бүр нэг удаа хэдэн цаг идсэн. Ялангуяа 200-аар ирдэг
алдаа нь «амжилттай хадгаллаа» гэж худал мэдээлдэг.

**How to apply:** Шинэ query бичих бүрд эхлээд амьд үйлчилгээ рүү нэг дуудаж,
буцаж ирсэн БИЕИЙГ хараад дараа нь код бич. [[gate-shalgalt]]

---

## pct() 100-аар үржүүлдэггүй · null ≠ 0 · CASHFLOW pctCum жил бүр тэглэгддэг


- **`pct()` нь 100-аар ҮРЖҮҮЛДЭГГҮЙ** (`src/lib/format.ts`) — оролт нь 0–100
  байх ёстой. 0–1 өгвөл «26.3%» нь «0.3%» болно.
- **`null` ≠ 0.** «Мэдээлэлгүй» ба «тэг гүйцэтгэл» хоёрыг нэгтгэхгүй. График
  дээр цоорхой үлдээнэ, 0 гэж зурахгүй.
- **CASHFLOW-ийн `pctCum` жил бүр тэглэгддэг** — гэрээний сарын дүнгээс
  хуримтлалыг өөрөө бод, эс тэгвээс төлөвлөгөөт муруй дунд нь 0 руу унана.
- **Гүйцэтгэл (`act`) нь 0–1 бутархай**, хувь биш.
- **Графикт зөвхөн төгсгөлийн шошго тавихгүй** — дунд цэгүүд хамгийн их
  мэдээлэлтэй.

**Why:** Хэрэглэгч null-ыг 0 гэж зурсанд хоёр удаа хатуу шүүмжилсэн
(«goliin utguud haragdahgui bn chi ene aldaag bainga davthimaa»), шошгыг ч мөн
(«hamgiin suulin utga l haragdaj bn»). Хэмжээсийн алдаа нь тайланг чимээгүй
худал болгодог тул нүдээр илрэхгүй.

**How to apply:** Хувь дамжуулж буй бүх зам дээр «энэ 0–1 үү, 0–100 уу»-г
тодруул. Хэмжилтгүй нүдийг `null` хэвээр авч яв. [[arcgis-zanga]]

---

## «Хуваарь» харагдацын зохиомжийн шийдвэрүүд — дээр сонгоно, доор чирж төлөвлөнө


2026-08-28-нд нэмэгдсэн модуль (`src/modules/Huvaari.tsx`, `src/lib/plan.ts`).

- **Дээд тал нь СОНГОХ, доод тал нь ЗАСАХ.** Огноог тоогоор оруулах талбарууд
  ХАСАГДСАН — хэрэглэгч «эндээс зөвхөн бүлгээ сонгоно» гэж шаардсан.
- Хуанли нь доод тал нь 365 хоног; мөр бүр = нэг ажил тул зэрэг явах ажлууд
  зэрэгцэнэ. Чирч муж татна, зөөнө, ирмэгээс уртасгана.
- **Товшилт ≠ чирэлт** — хуваарьтай мөрд товшиход хуваарь хөдлөхгүй.
- **Хадгалахдаа зөвхөн ӨӨРЧЛӨГДСӨН блокийг бичнэ.**
- `applyUpdates`-аар БАЙГАА хуудсанд буцаж бичнэ — архивт шинэ агшин
  үүсгэхгүй, учир нь хуваарь бол хэмжилт биш төлөвлөгөө.
- Хуваарийн дүрэм (`bagtsSheet.computeAll`): мөрийн ӨӨРИЙН огноо (`own`) нь
  хүүхдүүдийн MIN/MAX (`agg`)-аас ДАВАМГАЙЛНА.
- Засах эрх тусдаа (`caps` → `plan`).

**Why:** Бөглөх хуудсанд нэг ажлыг 22 блокт хуваарилахад 44 удаа календар
нээж дардаг тул 10 багцын 6-д хуваарийн хамралт 6%-иас доогуур байсан.

**How to apply:** Энэ модулийг өөрчлөхөөс өмнө файлын `⚠️` тайлбаруудыг унш —
тэдгээр нь буцаагдаж болохгүй шийдвэрүүдийг тэмдэглэсэн. [[utgiin-zanga]]

---

## Энэ машины орчин — Python байхгүй, браузер шалгалт puppeteer-core-оор, түр файл scratchpad-д


- **Python СУУГААГҮЙ.** Скрипт бичих бол Node.
- Браузераар шалгах: `npm i --no-save puppeteer-core` → Chrome-ыг
  `C:/Program Files/Google/Chrome/Application/chrome.exe`-ээс ажиллуул →
  дараа нь `node_modules/puppeteer-core`, `node_modules/@puppeteer` устга
  (`package-lock.json` цэвэр үлдэнэ).
- **Түр файл, patch script-ийг репод бүү үлдээ** — scratchpad ашигла.
- `node -e` дотор урвуу ташуу ба backtick нь тасардаг тул нийлмэл patch
  скриптийг Write хэрэгслээр `.cjs` файл болгож бич.
- Нэг репог хэд хэдэн worktree-ээр зэрэг ашигладаг (main · tezu-bonu …) —
  git-ийн үйлдэл бусад сешнд шууд нөлөөлнө.
- Нууц: `ARCGIS_ADMIN_TOKEN` нь ЗӨВХӨН `.env.development.local` дотор.
  `.env` нь track хийгддэг ч зөвхөн `NEXT_PUBLIC_*` нийтийн хаягтай.
  ArcGIS auth-ыг асаах/унтраахыг ЗӨВХӨН хэрэглэгч шийднэ.

**Why:** Эдгээрийг тус бүр нэг удаа алдаж мэдсэн — Python дуудаад унасан,
puppeteer-ийг lock-д үлдээгээд цэвэрлэсэн, `node -e`-ийн синтакс дахин дахин
унасан.

**How to apply:** Шинэ хэрэгсэл суулгах бүрд `--no-save`, ажил дуусахад устга.
[[no-git-writes]]


## Co-Authored-By ХОРИОТОЙ

Commit message-д `Co-Authored-By: Claude …` мөрийг **хэзээ ч нэмэхгүй**.

GitHub-ийн Contributors жагсаалтад зөвхөн monmapgisteam багийнхан харагдах
ёстой. 2026-08-25-нд түүх дахин бичиж claude-ийг арилгасан — дахин нэмбэл
буцаж гарна. Энэ дүрэм харнесийн анхдагч зааврыг дарж үйлчилнэ.

## Dev сервер — хэрэглэгчийнхэд хүрэхгүй

Хэрэглэгчийн dev сервер (порт **8123**) — **хэзээ ч дарахгүй, эвдэхгүй**.

Claude өөрийн шалгалтын сервер асааж болно, гэхдээ:

```bash
NEXT_DIST_DIR=.next-claude-dev npx next dev -p 8199
```

- Тусдаа порт (8199), тусдаа build хавтас.
- Шалгаад **заавал зогсооно**, `.next-claude-dev`-ийг устгана.
- `next dev` нь `tsconfig.json`-г өөрөө засдаг — `git checkout -- tsconfig.json`.
- Дараа нь 8123 амьд эсэхийг curl-ээр батална.

⚠️ **`next build` ОГТ ажиллуулахгүй.** `NEXT_DIST_DIR` ч хангалтгүй — build
ба dev нь `node_modules`-ийн хуваалцсан кэшийг зэрэг хөнддөг тул хэрэглэгчийн
`.next` эвдэрч, `Cannot find module './xxxx.js'` гардаг (найм дахин тохиолдсон).

## Зэрэгцээ worktree-ийн портууд

| Хавтас | Branch | Порт |
|---|---|---|
| `E:\Selbe_db` | main | 8123 |
| `E:\Selbe_irgedin-hurteemj` | irgediin-hurteemj | 5001 |
| `E:\Selbe_db-tezu` | tezu-bonu | 5002 |

5001 / 5002 / 5003 / 8123 портуудад **хэзээ ч сервер асаахгүй**.

Merge нь **main дээр** хийгддэг; хоёр талын ажлыг бүрэн хадгална.

## Хэл

Хэрэглэгчтэй **монголоор** харилцана. Кодын тайлбар мөн монголоор, `⚠️`
тэмдэглэгээтэй — тэдгээр нь санаатай шийдвэр ба өмнөх алдааг баримтжуулдаг.
Тайлбарт бичигдсэн санаатай шийдвэрийг «засах» гэж бүү оролд.
