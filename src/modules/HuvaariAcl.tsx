'use client';

/**
 * ХУВААРИЙН ЭРХ ТОХИРУУЛАХ ПАНЕЛ — БАГЦААР.
 *
 * ⚠️ ЛОГИК НЬ `ScopedAclPanel.tsx`-Д (2026-09-10). Энэ файл ба `ObyemAcl.tsx`
 * хоёр нь нэрээс бусад бүрэн ижил ~315 мөр байсныг нэг бүрэлдэхүүн болгов —
 * тэр давхардлаас 2026-09-08 · 09-нд НИЙТ 9 алдаа гарсан («засвар нь ижил
 * кодын НЭГД нь л хүрч, бусад руу хуулагдаагүй»). Энд зөвхөн ЯЛГАА нь
 * үлдэнэ: үүргийн нэр, шошго, зургаан текст.
 *
 * ⚠️ БАГЦ БҮРД ХОЁР ҮҮРЭГ:
 *   · Зохиогч  — хуваарийг төлөвлөж, батлуулахаар илгээнэ
 *   · Батлагч  — илгээгдсэнийг батлах / буцаах
 * Нэг багцад хэдэн ч аккаунт байж болно (ээлж, чөлөө). Нэг хүн өөр өөр
 * багцад өөр үүрэгтэй байж болно — тэр нь ЭНЭ бүтцийн гол давуу тал.
 *
 * ⚠️ Нэг хүн НЭГ БАГЦАД хоёр үүрэгтэй байвал ӨӨРИЙГӨӨ БАТЛАХ зам нээгдэхгүй:
 * `huvaariBatlah.decidePlan` зохиогч=батлагч тохиолдлыг татгалзана. Гэхдээ
 * панел нь анхааруулга харуулна — админ мэдэлгүй тийм тохиргоо хийвэл тэр
 * багц бүхэлдээ гацна (илгээх хүн бий, батлах хүн үгүй).
 */

import { t as tr } from '@/lib/i18nCore';
import {
  huvaariFailedUsers, listHuvaariAssigns, removeHuvaariAssign, setHuvaariGrants,
  subscribeHuvaariAcl, type PlanRole,
} from '@/lib/huvaariAcl';
import { ScopedAclPanel, type AclPanelSpec } from './ScopedAclPanel';

/**
 * ⚠️ Шошго ба зурвасыг ФУНКЦ болгож өгнө — модулийн түвшинд `tr()` дуудвал
 *    хэл солиход шинэчлэгдэхгүй (зурагдах агшинд дуудагдах ёстой).
 */
const SPEC: AclPanelSpec<PlanRole> = {
  roles: ['author', 'approver'],
  roleLabel: (r) => (r === 'author' ? tr('Зохиогч') : tr('Батлагч')),
  emptyLabel: (r) => (r === 'author' ? tr('Зохиогч томилоогүй') : tr('Батлагч томилоогүй')),

  list: listHuvaariAssigns,
  failedUsers: huvaariFailedUsers,
  subscribe: subscribeHuvaariAcl,
  setGrants: setHuvaariGrants,
  remove: (user) => removeHuvaariAssign(user),

  notes: () => [
    tr('Багц тус бүрд Зохиогч ба Батлагч аккаунтыг тусад нь томилно. Нэг багцад хэдэн ч аккаунт байж болно.'),
    tr('Хуваарилалт ArcGIS дээрх хуваалцсан хүснэгтэд хадгалагдаж, тухайн хүн өөрийн төхөөрөмжөөс нэвтрэхэд шууд үйлчилнэ. Үүрэг олгоход харгалзах эрх ба «Хуваарь» харагдац автоматаар нээгдэж, хасахад буцаагдана.'),
    tr('⚠️ Гүйцэтгэлийн урсгалаас ТУСДАА: хуваарийн үүрэг олгосон нь гүйцэтгэл зөвшөөрөх эрх өгөхгүй.'),
  ],
  confirmRemoveAll: (user) =>
    tr('«{0}»-г хуваарийн хуваарилалтаас бүрэн хасах уу? «Хуваарь төлөвлөх» ба «Хуваарь батлах» эрх нь мөн буцаагдана.', user),
  stuckMsg: () =>
    tr('⚠️ Батлагч нь зохиогчтой ижил хүн — өөрийн илгээснийг өөрөө батлах боломжгүй тул энэ багцын хуваарь гацна. Өөр батлагч нэмнэ үү.'),
  noApproverMsg: () =>
    tr('⚠️ Батлагч томилоогүй — илгээсэн хуваарийг хэн ч батлахгүй.'),
};

export function HuvaariAcl() {
  return <ScopedAclPanel spec={SPEC} />;
}
