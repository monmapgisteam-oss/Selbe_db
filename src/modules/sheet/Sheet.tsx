'use client';

import FillNew, { type SheetView } from "./FillNew";
import { Icon } from "@/components/Icon";
import { t as tr } from "@/lib/i18nCore";
import st from "./sheet.module.css";

/**
 * ГҮЙЦЭТГЭЛ БӨГЛӨХ — өгөгдөл оруулах ПОРТАЛ.
 *
 * ⚠️ 2026-08-18 (хэрэглэгчийн шийдвэр): дөрвөн табтай байсныг ЗӨВХӨН «Гүйцэтгэл
 * шинэ» (FillNew) болгож хумив — энэ нь өгөгдөл ОРУУЛДАГ цор ганц хэсэг, бусад
 * гурав (Pivot/Wbs/Level5) нь харах/экспортын хуудаснууд байсан. Тэдгээрийн код
 * ба ТООЦООЛОЛ (`bagtsSheet.ts → computeAll`, excel-ийн томъёонууд) ХЭВЭЭР —
 * FillNew дотор ажилладаг хэвээр, зөвхөн навигаци нь хасагдсан.
 */
export function Sheet({ view }: { view?: SheetView } = {}) {
  return (
    <div className={st.tabs}>
      {/* Порталын толгой — таб биш, өгөгдөл оруулах хэсгийн тодорхойлолт */}
      {!view && (
      <header className={st.portalHead}>
        <span className={st.portalIcon}><Icon name="pen" size={16} /></span>
        <div className={st.portalText}>
          <h2 className={st.portalTitle}>{tr('Гүйцэтгэл бөглөх')}</h2>
          <p className={st.portalDesc}>
            {/* ⚠️ 2026-09-25 (хэрэглэгчийн ЭЦСИЙН шийдвэр, баталгаажсан — өмнөх «НИЙТ обьём» зааврыг БУЦААВ):
                нүдэнд ӨМНӨХ БӨГЛӨЛТӨӨС ХОЙШ хийсэн обьём бичнэ; нийт нь (архивын утга + нэмэлт) автоматаар
                бодогдож батлахад архивт бичигдэнэ (`bagtsSheet.CellMode` · `incCell`). */}
            {tr("Багцын гүйцэтгэлийн өгөгдөл оруулах портал — блок бүрийн нүдэнд ӨМНӨХ БӨГЛӨЛТӨӨС ХОЙШ хийсэн обьёмоо бичнэ; нийт обьём, гүйцэтгэлийн хувь, нийлбэр, жин, дүн автоматаар бодогдоно.")}
          </p>
          {/*
            * ГУРВАН АЛХМЫН ЗААВАР (2026-09-15-ны хэрэглээний аудит).
            *
            * ⚠️ ЯАГААД: шинэ гүйцэтгэгч хуудас нээхэд 1,400 мөр, 60 багана
            *    харагдана; нүд дарвал «Засвар хаалттай — дээрх "Бөглөх" товчийг
            *    дарж нээнэ үү» гэсэн мэдэгдэл авна. Тэр товч нь хэрэгслийн
            *    мөрөнд 12 элементийн дунд байдаг тул шинэ хүн гүйлгэж хайна.
            *    Онбординг, заавар огт байгаагүй.
            *
            * ⚠️ Нэг мөр, дөрвөн алхам — урт заавар нь өөрөө уншигдахгүй.
            */}
          <p className={st.portalSteps}>
            {/* ⚠️ 2026-09-25: ③ — «энэ удаад хийсэн» (нэмэлтийн горим) */}
            {tr('① Багцаа сонго → ② «Бөглөх» дар → ③ нүдэнд энэ удаад хийсэн обьёмоо бич → ④ «Илгээх»')}
          </p>
        </div>
      </header>
      )}
      <FillNew view={view} />
    </div>
  );
}
